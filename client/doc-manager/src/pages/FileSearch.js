import { Container, Typography } from "@mui/material";

const FileSearch = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>File Search</Typography>
      {}
    </Container>
  );
};

export default FileSearch;
